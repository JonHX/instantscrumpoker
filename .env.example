# API Configuration
# Copy this file to .env.local and update with your values
#
# For production, defaults use Cloudflare domain: api.instantscrumpoker.com
# For local dev, you can use direct API Gateway URLs or the Cloudflare domain

# HTTP API Gateway Base URL (WITH /api suffix - the API client adds /rooms, /rooms/{id}, etc.)
# Production (via Cloudflare): https://api.instantscrumpoker.com/api
# Direct API Gateway: https://v4efb7cuy3.execute-api.eu-west-1.amazonaws.com/api
NEXT_PUBLIC_API_BASE_URL=https://api.instantscrumpoker.com/api

# WebSocket API Gateway Endpoint
# Production (via Cloudflare): wss://api.instantscrumpoker.com/dev
# Direct API Gateway: wss://me9ybopkak.execute-api.eu-west-1.amazonaws.com/dev
# Note: WebSockets may need to use direct API Gateway URL if Cloudflare doesn't proxy WebSockets
NEXT_PUBLIC_WS_ENDPOINT=wss://api.instantscrumpoker.com/dev
